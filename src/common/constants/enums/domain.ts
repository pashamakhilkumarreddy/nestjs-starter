export enum SortByTypes {
  none = 'none',
  ASC = 'asc',
  DESC = 'desc'
}

export enum FilterTypes {
  USERS = 'users'
}

export enum SORT_TYPES {
  USERS = 'users'
}

export enum RedisPrefixEnum {
  USER = 'user'
}
