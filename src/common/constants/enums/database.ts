export enum AuthTypes {
  GOOGLE = 'google',
  MICROSOFT = 'microsoft',
  OKTA = 'okta',
  LOCAL = 'local'
}

export enum UserStatus {
  ACTIVE = 'active',
  DISABLED = 'disabled'
}

export enum Status {
  ACTIVE = 'active',
  DISABLED = 'disabled'
}

export enum RoleTypes {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  USER = 'user',
  OTHER = 'other'
}
